<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="norton.animademo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".daimajiaviewanimdemo.ViewAnimDemoActivity" />
        <activity android:name=".objball.RotationActivity" />
        <activity android:name=".bezier.BezierActivity" />
        <activity android:name=".bezier.ShopingCarActivity" />
        <activity android:name=".bezier.BubbingActivity" />
        <activity android:name=".objball.BallInterpolatorActivity" />
        <activity android:name=".objball.BallTypeEvaluatorActivity" />
        <activity android:name=".viewpager.ViewPagerActivity" />
        <activity android:name=".activitytransformer.OneActivity" />
        <activity android:name=".activitytransformer.TwoActivity" />
        <activity android:name=".activitytransformer.ThreeActivity" />
        <activity android:name=".fragmenttransformer.FragmentTransformerActivity"></activity>
    </application>

</manifest>